#Dev

1. Clonar el .env.template y crear el .env
2. Ejecutar npm install
3. Ejecutar npm run dev para levantar el server.

#Postgress

1. Ajustar docker-compose si es necesario (Puertos)
2. rellenar las variables de entorno
3. Lanzar docker compose up -d
